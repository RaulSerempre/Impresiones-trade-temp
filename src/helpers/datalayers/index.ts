import useDatalayersConfig from "./config";
import datalayerValues from "./general";

const useHelpers = () => {
  return {
    useDatalayersConfig,
    datalayerValues
  };
};

export default useHelpers;
